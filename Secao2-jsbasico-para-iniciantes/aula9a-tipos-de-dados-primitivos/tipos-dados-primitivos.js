// String, Number, Undefined, Null, Boolean, symbol
const nome = "Izaak" // String
const nome1 = 'Izaak' // String
const nome2 = `Izaak` // String
const num1 = 10 // Number
const num2 = 10.25 // Number
let nomeAluno // Underfined -> não aponta pra local nenhuma na memória
const sobrenomeAluno = null // Nulo -> não aponta pra local nenhuma na memóriaff
const aprovado = false // Boolean = true, false (lógico)

console.log(typeof aprovado, aprovado)
